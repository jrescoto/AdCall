<?php
App::uses('AppModel', 'Model');
/**
 * Metrics Model
 * notes: 
 * 1. no table has been used
 * 2. this is implemented to follow the MVC pattern and to allow creation of form through FormHelper
 *
 */
class Metrics extends AppModel {
    var $useTable = false;
}
